# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Keep README and .gitkeep in videos; video files (*.mp4 etc.) are now committed so they work on Vercel.
# If the repo gets too large, remove videos and host them on Vercel Blob or Cloudinary instead.
# Source folder for copy-assets – do not commit (only hero.mp4 and card-1/2/3.mp4 are needed)
/public/videos/Raw vids/
# Videos exceed GitHub 100MB limit – keep locally; for deploy use Vercel Blob or external hosting
/public/videos/*.mp4
/public/videos/*.MP4
/public/videos/*.mov
/public/videos/*.webm
!public/videos/README.md
!public/videos/.gitkeep
